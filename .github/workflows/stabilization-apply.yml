name: Stabilization Patch Applicator

on:
  push:
    branches:
      - hivecode/wo-0002c-stabilization

permissions:
  contents: write

jobs:
  apply:
    if: ${{ !contains(github.event.head_commit.message, '[stabilization-applied]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: hivecode/wo-0002c-stabilization
          fetch-depth: 0
      - name: Apply bounded patches
        run: python3 scripts/stabilization_wave1.py
      - name: Commit corrections when needed
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No patch delta"
            exit 0
          fi
          git config user.name "KayzenRoot"
          git config user.email "114633702+KayzenRoot@users.noreply.github.com"
          git add crates/goose/src/acp/server/load_session.rs crates/goose/src/recipe/mod.rs
          git commit -m "fix(stability): close first critical session and recipe gaps [stabilization-applied]"
          git push origin HEAD:hivecode/wo-0002c-stabilization
