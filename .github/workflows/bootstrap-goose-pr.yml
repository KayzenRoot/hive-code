name: Bootstrap Goose Import PR

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  import-goose-main:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Import Goose history without upstream workflow files
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          python -m pip install --disable-pip-version-check --quiet git-filter-repo
          git clone --bare --single-branch --branch main https://github.com/aaif-goose/goose.git goose.git
          cd goose.git
          git filter-repo --force --path .github/workflows --invert-paths
          git remote add hive "https://x-access-token:${GH_TOKEN}@github.com/KayzenRoot/hive-code.git"
          git push hive refs/heads/main:refs/heads/goose-main
